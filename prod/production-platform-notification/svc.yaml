
apiVersion: v1
kind: Service
metadata:
  name: production-platform-notification-svc
  namespace: production
  labels:
    service: production-platform-notification
spec:
  selector:
    dep: production-platform-notification
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
