
apiVersion: v1
kind: Service
metadata:
  name: production-cloud-provider-driver-svc
  namespace: production
  labels:
    service: production-cloud-provider-driver
spec:
  selector:
    dep: production-cloud-provider-driver
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
