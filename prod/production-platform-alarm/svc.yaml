apiVersion: v1
kind: Service
metadata:
  name: production-platform-alarm-svc
  namespace: production
  labels:
    service: production-platform-alarm
spec:
  selector:
    dep: production-platform-alarm
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
