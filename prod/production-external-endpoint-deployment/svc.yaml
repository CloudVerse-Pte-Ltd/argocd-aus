apiVersion: v1
kind: Service
metadata:
  name: production-external-endpoint-svc
  namespace: production
  labels:
    service: external-endpoint
spec:
  selector:
    dep: external-endpoint
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
