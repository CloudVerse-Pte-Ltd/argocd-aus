apiVersion: v1
kind: Service
metadata:
  name: production-platform-budget-svc
  namespace: production
  labels:
    service: production-platform-budget
spec:
  selector:
    dep: production-platform-budget
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
